
#include <iostream>

using namespace std;

int main(){
    int a[5];
    cin >> a[0] >> a[1] >> a[2] >> a[3] >> a[4];
    int k;
    cin >> k;
    for(int i = 0;i < 5;i++){
        for(int j = 0;j < 5;j++){
            if(a[i] - a[j] > k && a[i] - a[j] > -k){
                
            }
        }
    }
}