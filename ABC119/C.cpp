
#include <iostream>
#include <vector>

using namespace std;

int main(){
    int N;
    int A,B,C;

    cin >> N >> A >> B >> C;

    vector<int> length(N);

    for(int i = 0;i < N;i++){
        cin >> length(i);
    }
    for(int i = 0;i < N;i++){
        
    }
}
